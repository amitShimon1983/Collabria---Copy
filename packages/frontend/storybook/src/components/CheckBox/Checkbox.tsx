import React from 'react';
import { Checkbox as CheckboxPersona, ICheckboxProps } from '@fluentui/react';

const Checkbox = (props: ICheckboxProps) => {
  return <CheckboxPersona {...props} />;
};

export default Checkbox;
